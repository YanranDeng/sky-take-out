<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.SetmealDishMapper">
    <insert id="saveSetmealDishes">
        insert into setmeal_dish(setmeal_id, dish_id, name, price, copies) VALUES
        <foreach collection="setmealDishes" separator="," item="item">
            (#{item.setmealId}, #{item.dishId},#{item.name},#{item.price},#{item.copies})
        </foreach>
    </insert>
    <delete id="deleteBySetmealId">
        delete from setmeal_dish where setmeal_id in
        <foreach collection="setmealIds" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

    <select id="getSetmealIdByDishId" resultType="java.lang.Long">
        select setmeal_id from setmeal_dish sd where dish_id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </select>
    <select id="getBySetmealId" resultType="com.sky.entity.SetmealDish">
        select sd.*
        from setmeal s
                 left join setmeal_dish sd on s.id = sd.setmeal_id
        where s.id = #{id}
    </select>
</mapper>